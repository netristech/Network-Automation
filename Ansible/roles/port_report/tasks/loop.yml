---
- name: Set Hostname
  set_fact:
    interface: "{{ item.key }}"
  when: item.key is defined
  
- name: Set Status
  set_fact:
    status: "{{ item.value.state }}"
  when: item.value.state is defined
  
- name: Print Results
  debug:
    msg: "{{ interface }}, {{ status }}"
